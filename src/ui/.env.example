# DeepTrade UI Configuration

# Set to "true" ONLY when deploying a public promotional/showcase website (e.g. on Vercel)
# When "false" (default), "Initialize Terminal" routes directly to /login -> /chat for live trading.
VITE_PROMO_MODE=false

# Typography
VITE_APP_FONT="Bricolage Grotesque"
